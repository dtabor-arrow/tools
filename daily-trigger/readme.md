# Daily Trigger of FlexReports

This can be daily, weekly, hourly, whatever you want.

The script reads a list of FlexReports and executes an API call to run them.  This will ensure that the listed reports are current in CloudHealth and avoid needing to run them manually.

